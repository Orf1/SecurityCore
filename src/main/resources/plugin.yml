name: SecurityCore
version: ${project.version}
main: com.orf1.securitycore.Main
api-version: 1.13
load: STARTUP
authors: [Orf1]
description: Simple plugin for keeping your server secure!
commands:
  securitycore:
    description: Used to run all the Security Core commands.
    aliases:
      - sc
      - security
      - secureityc
  login:
  register:
  authenticate:
  setup:
  reset: